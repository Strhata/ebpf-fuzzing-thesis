# Colab training-side dependencies for rl_grpo.py --remote-reward-url
# torch is pre-installed on Colab runtimes; not listed here.
#
# PINNED to the local env these scripts were written/tested against. The code
# carries version-specific workarounds — DO NOT loosen without re-testing:
#   * _fixed_unwrap_model_for_generation  → transformers 5.x KV-cache fix
#   * trl.import_utils.is_vllm_available patch + use_vllm=False → trl 0.14.x
#   * model.warnings_issued pre-seed       → trl 0.14 / peft 0.18 proxy quirk
transformers==5.8.1
trl==0.14.0
peft==0.18.1
accelerate==1.13.0
bitsandbytes==0.49.2
datasets==4.0.0
wandb==0.26.1
huggingface_hub==1.14.0
requests==2.34.0
