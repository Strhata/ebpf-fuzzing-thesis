# VM images and compiled kernels (arch-specific, built by make setup)
*.qcow2
*.img
*.iso
bzImage*
fuzzing/bzImage*
fuzzing/*.id_rsa
fuzzing/*.id_rsa.pub
fuzzing/trixie/
data/corpus/buzzer
data/corpus/ebpf_validator

# Kernel source cache (downloaded by make build-kernel)
build/

# Models (on HuggingFace)
modello_*/
adattatore_ebpf_*/

# Dataset corpus (on HuggingFace)
shared_corpus/
*.jsonl
*.jsonl.gz

# Checkpoints (on HuggingFace)
checkpoints/*/

# Python / Jupyter
.pixi/
__pycache__/
*.pyc
.ipynb_checkpoints/

# Local secrets
.env

# WandB local run logs (synced to cloud)
wandb/

# Scratch / planning docs
.scratch/

# Local-only writing style guide (never committed)
thesis/STYLE.md

# Claude Code
CLAUDE.md
.claude/

# LaTeX build artifacts
thesis/**/*.aux
thesis/**/*.log
thesis/**/*.out
thesis/**/*.toc
thesis/**/*.bbl
thesis/**/*.blg
thesis/**/*.fls
thesis/**/*.fdb_latexmk
thesis/**/*.synctex.gz
thesis/**/*.bcf
thesis/**/*.run.xml
thesis/**/*.pdf
# ...but figure assets under thesis/figures/ are source, not build output — keep them
!thesis/figures/
!thesis/figures/**

# Knowledge graph output
graphify-out/

# Windows Zone.Identifier metadata
*:Zone.Identifier

# Misc
*.pid
*.log
diagnostic_data/
results/
tools/kcov_validator/kcov_validator
data/corpus/kcov_validator
tools/ebpf_validator/ebpf_validator

# large per-program benchmark dumps (full PC traces); keep the _report.md + summary.csv
benchmarks/runs/*_run.json

# Exam slide deck (issue #38) — build artifacts, kept local
slides/
